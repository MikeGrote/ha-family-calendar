[pytest]
pythonpath = .
testpaths = .

# Der Einstellungsspeicher ist durchgehend asynchron. Ohne diese Zeile
# ueberspringt pytest die Tests nicht etwa - er laesst sie scheitern, was
# immer noch besser ist, als sie stillschweigend gruen zu melden.
asyncio_mode = auto
